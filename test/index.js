describe('API Initialization', async () => {

  it('Should start the test suite', () => {
    axios.get('/your/endpoint') {
      results = res
    }


    expect(results).to.equal({

    })
  })

  it('Needs to fetch and return CSRF Token')

})
